// ============================================
// Poriborton Foundation — frontend config
//
// One place to point the site at the backend once it's deployed.
// Until API Gateway + Lambda are live (see CLAUDE.md "AWS deployment"
// checklist), this stays empty and the forms show a friendly
// "not connected yet" message instead of failing silently.
//
// After deploying, set this to your API Gateway invoke URL, e.g.:
//   const API_BASE_URL = 'https://abc123.execute-api.ap-southeast-1.amazonaws.com';
// ============================================
const API_BASE_URL = '';
